<h2>Update Member</h2>

<%= form_for(:member, :url => {:action => 'update', :id => @member.id}) do |f| %>
  <%= render(:partial => "form", :locals => {:f => f}) %>

  <div>
    <%= submit_tag("Update Member") %>
  </div>

<% end %>

